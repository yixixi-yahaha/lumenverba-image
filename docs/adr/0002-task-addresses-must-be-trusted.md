# 任务地址必须可信

异步任务地址只有在解析为 `api.lumenverba.cc` 的 HTTPS、使用有效端口 443 且位于 `/v1/` 内时，才可接收 API Bearer 密钥。支持相对地址；拒绝协议相对地址、用户信息、fragment、其他端口、其他主机和 API 命名空间外的路径，防止认证信息泄露。携带 Bearer 的请求拒绝自动重定向，避免已验证地址将认证信息或任务请求引向未验证目标。

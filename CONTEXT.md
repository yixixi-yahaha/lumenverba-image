# Lumenverba 图像技能

本上下文定义 Lumenverba 图像请求、结果交付与版本发布使用的统一语言。

## Language

**创建请求**:
要求 Lumenverba 创建一张或多张新图片的计费请求；网络异常后其结果可能处于未知状态。
_Avoid_: 生成调用、POST 请求、可重试请求

**读取请求**:
获取已有创建请求状态或结果且不会创建新图片的请求。
_Avoid_: 无害请求、轮询调用、下载调用

**可信任务地址**:
位于 Lumenverba 已认证 API 源和版本命名空间内的异步任务地址。
_Avoid_: HTTPS 地址、回调 URL、任意任务 URL

**结果回执**:
一次技能调用生成的版本化记录，包含图片路径、错误、退出码与完成状态。
_Avoid_: 日志文件、输出目录扫描、推断结果

**发布源**:
公开的 `main` 分支；技能发布只能从其已验证内容创建。
_Avoid_: 发布分支、本地检出、带标签的功能分支

**发布门禁**:
创建发布前必须通过的离线验证与人工真实 API 冒烟测试。
_Avoid_: 尽力检查、仅 CI、仅单元测试

**输出尺寸**:
`gpt-image-2` 创建结果的像素尺寸；可以交给模型自动选择，也可以由用户指定符合模型约束的宽度与高度。
_Avoid_: 画质、质量档位、分辨率档位

**分辨率档位**:
技能为常用输出尺寸提供的语义名称；档位最终解析为一个具体输出尺寸，不是 API 参数值。
_Avoid_: size 参数、CLI 选项、模型质量

**实验分辨率**:
总像素超过 `3,686,400`（`2560x1440`）的显式输出尺寸；允许使用，但稳定性低于常规输出尺寸，执行前必须提示风险。
_Avoid_: 无效尺寸、禁止尺寸、4K 模式
